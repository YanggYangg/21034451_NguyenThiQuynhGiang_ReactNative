/**
 * Filter --> myFilter, find, every, some, map, reduce, any, forEach
 */

//1.Filter
const ages = [4, 11, 42, 14, 39, 5];
const filteredAge = ages.filter(ages => ages > 10);
console.log(filteredAge);

